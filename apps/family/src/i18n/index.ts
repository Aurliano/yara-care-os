import { fa } from "./fa";

export const t = fa;

export { toPersianDigits, toLatinDigits } from "./numerals";
export { formatClock, formatRelative, formatPersianDate } from "./dates";
export {
  careActivityStatusLabel,
  completionStateLabel,
  entitlementLabel,
  occurrenceStatusLabel,
} from "./labels";
