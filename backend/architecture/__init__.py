"""Architecture guardrails for the modular monolith.

This package provides reusable checks that grow with B1–B11:

- cross-domain import rules (via import-linter contracts)
- cross-domain FK rules (via model_relations helpers)
"""
